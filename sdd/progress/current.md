# Current SDD Work

Active feature: `ZIN-SDD-040 crm-core-flow-simplification`

Status: `in_progress`

Slice 1 status: completed and verified in staging on 2026-07-31.

Slice 2 status: completed (T6 to T9) and verified in staging on 2026-07-31.

Slice 3 status: completed; T10 to T14 are implemented. T12 to T14 were verified in staging on 2026-07-31 with migrations `20260731162336_crm_proposal_opportunity_workflow.sql`, `20260731181410_proposal_acceptance_integrity.sql` and `20260731191249_complete_crm_activation.sql`, plus regenerated staging types.

Slice 4 status: in progress; T15 and T17 are implemented and verified in staging. The T16 technical foundation is implemented and verified in staging; approved business percentages, the five direct-partner assignments and real marketer/product decommission terms remain pending.

T12 evidence: proposal send and public acceptance now lock proposal/opportunity rows, preserve rate limiting and signature validation, return stable success under repeated/racing acceptance, and carry the proposal owner/opportunity into commission, task and pending-contract effects. The staging privilege/integrity verifier passed with zero mismatches among linked proposals. The safe legacy report found two pre-existing active proposals without OCR or opportunity linkage; they remain unchanged because they cannot be associated unambiguously.

T13 evidence: `proposal_acceptance_integrity` is an admin-only `security_invoker`/`security_barrier` view with no PII fields. The protected retry opens the activation state under row locks and retries only missing commission/contract effects without notifications. Staging verification found one historical accepted proposal with no opportunity; its existing activation, commission and contract are intact, and the admin queue correctly requires manual review instead of guessing a relationship. The other unlinked historical proposal remains `sent`, so it is not an acceptance-integrity item.

T14 evidence: activation is one compact admin action backed by the service-only `complete_crm_activation` transaction. It validates canonical proposal/opportunity/supply ownership and complete contract data, then atomically upserts the active contract, records activation and activity audits, advances `activation -> won` and activates the client. The staging verifier returned `ok`, types were regenerated, the migration dry-run reports up to date, 494 tests pass and desktop/mobile checks found no horizontal overflow.

T15 evidence: active contracts now create at most one renewal opportunity at the canonical 60-day threshold. Durable in-app reminders are emitted once at 60, 30, 7 and 0 days, while repeated cron execution remains idempotent. Unknown permanence appears in one role-scoped correction panel and is never converted into a guessed date. Structural and transactional staging verification passed, remote types were regenerated, the staging dry-run reports up to date, 508 tests pass, the production build succeeds and desktop/mobile checks found no horizontal overflow or console errors.

T16 design refinement: the Zinergia economic model now separates marketer gross revenue, commercial entitlement, franchise royalty and central remainder through immutable allocation snapshots. It adds versioned marketer decommission policies, append-only full/partial reversals, supplier-statement reconciliation, paid-balance offsets and rectifying-document requirements. Customer penalties remain independent from collaborator decomissions, and ambiguous historical economic rows must enter admin review rather than being guessed.

Zinergia channel refinement: the five current partners use an explicit `partner_direct` plan with a higher partner share, zero franchise share and smaller central share. Franchise-network commercials use a separate plan with a lower commercial share and higher Zinergia share, plus an optional explicit franchise allocation. Plans and assignments are versioned, admin-controlled and independent from authentication roles; no identities or percentages are hardcoded.

T16 technical implementation evidence: migration `20260731220129_commission_lifecycle_ledger.sql` adds versioned economic plans and explicit assignments, versioned marketer/product decommission policies and immutable non-overlapping bands, supplier-statement provenance, balanced frozen allocations, the six canonical lifecycle states, append-only events, reviewable full/partial adjustments, protected service-only workflows, activation-driven eligibility, reconciliation queues and deterministic legacy-state backfill. Proposal acceptance invokes protected lifecycle initialization after the compatible aggregate is created; absent business configuration is preserved as an admin reconciliation item instead of receiving invented percentages. Pure domain and migration-contract tests cover direct-partner/franchise allocation, cent balancing, state transitions, policy-driven reversal foundations and self-assignment denial. The migration was applied only to `zinergia-staging`; structural, transactional and RLS verification scripts each returned `ok`, remote types were regenerated and the staging dry-run reports up to date. The admin now has one focused `/admin/commissions` surface to create balanced, versioned direct-partner or franchise plans, assign them through the protected RPC and inspect reconciliation items without hardcoded identities. Migration `20260731224153_fix_acceptance_integrity_admin_helper.sql` also restores executable admin access to the acceptance-integrity queue through `private.is_superadmin()`; its authenticated staging verifier returned `ok`. Desktop and 390px E2E checks cover percentage balancing, disabled franchise share for direct partners, responsive containment, browser errors and WCAG A/AA. Final gates pass: 87 test files with 547 tests, TypeScript, lint, 42-route production build, SDD validation and diff check. T16 remains open until approved percentages/policies are inserted and the five partners are assigned as business data; staging currently has only two real profiles and no identities or percentages were invented.

T16 initial-configuration refinement: when either channel model is absent, `/admin/commissions` now opens one guided form for the direct-partner split, franchise-network split and selection of up to five existing partner profiles. Both central shares are calculated live; submission is disabled unless the direct partner receives more than the franchise commercial and Zinergia retains more in the franchise channel. `configure_commission_model` versions both plans and applies selected direct assignments in one protected transaction, rejects duplicate/self/missing profiles and remains executable by `service_role` only. Migration `20260801025504_configure_commission_model.sql` introduced the workflow; transactional staging verification detected an incorrect assignment-column identifier, and immutable history was preserved by correcting it in `20260801030120_fix_configure_commission_model_assignment.sql` instead of rewriting the applied migration. The repeated verifier returned `ok` with all writes rolled back, staging types were regenerated and the migration dry-run reports up to date. The form does not persist provisional percentages during E2E and remains available until the business configuration is explicitly saved.

T16 decommission-policy refinement: `/admin/commissions` now includes policy configuration during first setup and later management. An admin enters marketer, optional exact product code, consolidation days, clawback limit and one to twelve active-day bands; the UI derives each next start day and requires percentages to stay equal or decrease. Migration `20260801095258_configure_decommission_policy.sql` adds one service-role-only atomic workflow that locks policy versioning, rejects gaps, overlaps, incomplete coverage and increasing reversals, then inserts the immutable policy and bands together. The staging verifier created two successive synthetic versions, proved complete bands and effective privileges, rejected an invalid policy and rolled every write back before returning `ok`. Remote types were regenerated, staging is up to date, and desktop/mobile E2E covers the policy form without persisting provisional terms.

T17 commercial-workspace evidence: `/dashboard/commissions` is now the canonical daily surface for commercial users. It presents pending, eligible, validated, invoiced, paid and reverted states; totals commercial net amounts after adjustments; and exposes source proposal, client, marketer/product, reconciliation state and decomission cause, days active, frozen policy, original/reversed amounts, evidence and dispute status. Historical commissions recover the client through their proposal when the newer direct link is absent. The former `/dashboard/wallet` route is an HTTP compatibility redirect, navigation and notifications now target the canonical route, and no gamification or withdrawal controls remain in the primary workflow. Pure workspace tests and authenticated staging checks pass on desktop and 390px mobile with zero horizontal overflow, browser errors, Next overlays or Axe WCAG A/AA violations.

T17 is complete. The admin queues and atomic fiscal workflow are implemented and verified in staging, remote types are regenerated, and authenticated fiscal E2E passes on desktop and mobile. T16 business percentages, partner assignments and real marketer policy terms remain pending input through `/admin/commissions`.

T17 admin-queue refinement: `/admin/commissions` now defaults to an operational workspace with three explicit queues: eligible and reconciled commissions awaiting validation, validated and uninvoiced commissions awaiting settlement preparation, and proposed adjustments plus reconciliation blockers awaiting resolution. The economic model remains available as a secondary tab. Net settlement amounts subtract confirmed commercial reversals, fiscal readiness is visible, and already linked commissions are excluded from the settlement queue. Validation and adjustment resolution require a written reason and invoke only `transition_commission_lifecycle` or `resolve_commission_adjustment` through the service role after an admin guard. The unsafe legacy invoice generator is not exposed from the queue. Pure queue tests, action tests, component tests and authenticated staging E2E pass; desktop and 390px checks report no overflow, browser errors, overlays or Axe WCAG A/AA violations. The remaining T17 work is the atomic fiscal invoice and rectifying-document lifecycle.

T17 fiscal-lifecycle local evidence (2026-08-01): migration `20260801154820_atomic_fiscal_commission_invoicing.sql` adds central fiscal-entity configuration, protected fiscal-profile mutation, prior self-billing agreements, per-document acceptance, concurrency-safe issuer sequences, normalized commission lines with active uniqueness, rectification requests and service-only atomic draft/accept/issue/cancel/pay/rectify workflows. Drafts accept only validated, reconciled, positive, unreserved commissions owned by one commercial. Header totals equal rounded normalized lines; IVA applies to the full base and retention reduces the payable total. The commercial is always issuer and Zinergia recipient, including documents Zinergia creates under an accepted self-billing agreement. Cancelling a draft releases its reservation; issuing and paying require every normalized line to transition with the invoice or the transaction aborts. A confirmed decomission affecting an issued/paid document queues one linked rectifying draft instead of editing the original. Legacy browser multi-writes, hardcoded fake issuer data and direct fiscal-profile writes were removed. `/dashboard/invoicing` now uses one server load and only state/role-valid actions; `/admin/commissions` adds a fiscal form for the real Zinergia entity and self-billing agreements. Structural and rollback-only transaction verifiers are ready under `supabase/scripts/`. Local gates pass: 94 test files, 578 tests, TypeScript, lint, diff check and the 42-route production build. Docker is not running and this Codex process has no `SUPABASE_ACCESS_TOKEN`, so no PostgreSQL/staging pass or generated-type claim is recorded yet.

T17/T18 hardening continuation (2026-08-01): the pending fiscal migration now grants franchise supervisors read-only visibility over their commercial network's self-billing agreements, normalized fiscal lines and rectification requests while keeping all browser mutations revoked. The structural verifier checks effective `PUBLIC`, `anon` and `authenticated` privileges and the expected policies. The fiscal server-action RPC helper now binds the service client correctly; its focused regression test proves the Supabase client context is preserved. These changes remain local-only until the migration is applied to staging.

T19 staging evidence (2026-08-01): the public acceptance fixture is append-only and creates a fresh supply point, canonical `proposal_sent` opportunity and proposal for every run. It no longer deletes immutable commission/event history. The opt-in browser flow passed end to end with a drawn signature and exactly one pending commission, one documentation task and one pending-switch contract. Read-only public proposal checks passed 4/4. A broad non-fiscal staging run passed 59 tests with 6 intentional skips; the sole transient OCR callback failure was an HTML 5xx from concurrent Next.js development compilation, so the idempotent callback fixture now retries only 5xx/non-JSON responses and passed four concurrent repetitions. Full T19 remains open because activation-to-eligibility and validated-commission-to-fiscal-payment require the pending fiscal schema and approved business configuration.

T20 local gate evidence (2026-08-01): `npx tsc --noEmit`, `npm run lint`, `npm run test` (95 files, 580 tests) and `npm run build` (42 static/dynamic pages) pass. The Playwright server now honors a validated local `PLAYWRIGHT_BASE_URL` port, launches the checked-in Next CLI without shell interpolation and uses the same URL for readiness and browser navigation. Auth setup is serialized with 60-second route waits, and stale navigation/client selectors were aligned with the simplified shell.

T17/T18 staging closure (2026-08-02): Supabase access was restored and the fiscal migration was applied to `zinergia-staging`. Its transaction verifier exposed two PostgreSQL variable/column ambiguities; additive corrective migrations `20260802002600_fix_fiscal_sequence_year_ambiguity.sql` and `20260802002717_fix_rectifying_invoice_variable_ambiguity.sql` fixed them without rewriting applied history. The structural verifier returned zero findings and the rollback-only fiscal lifecycle returned `ok`. `20260802003127_fix_complete_activation_conflict_ambiguity.sql` and `20260802003128_harden_legacy_analytics_and_rgpd.sql` also corrected a legacy activation conflict, made analytics RPCs invoker-safe, pinned search paths and replaced the RGPD purge temp-table flow with one CTE. Remote `db lint` and the performance advisor now return zero findings. The security advisor retains four intentional RLS-without-policy INFO findings for service-only tables with browser privileges revoked, plus one project-setting warning because leaked-password protection is disabled. Authenticated desktop/mobile fiscal coverage, the OCR callback and the complete 69-test Playwright run pass with 63 executed and 6 intentional skips. Staging types were regenerated.

Current blockers (2026-08-02): T16 still requires approved percentages for both channels, the real five direct-partner assignments and marketer/product decommission terms. Those inputs are also required for the real activation-to-payment T19 story. Supabase Auth leaked-password protection must be enabled in project settings. Production promotion and production type regeneration in T20 are intentionally not attempted before those business inputs and final staging story are complete.
